import 'package:flutter/material.dart';

class adminchat extends StatefulWidget {
  adminchatState createState() => adminchatState();
}

class adminchatState extends State<adminchat> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(child: new Text("Chat is not implemented yet")),
    );
  }
}
